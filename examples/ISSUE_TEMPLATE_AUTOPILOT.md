<!--
Autopilot-friendly issue template.

- The AI agents read ONLY the title and this body. Comments are ignored:
  to clarify something, edit the body.
- The body is sent to the AI provider (Anthropic / OpenAI). Never paste
  secrets, credentials, tokens or personal data.
- Risk is NOT read from this text: set it with a label
  (`abi-autopilot mark-ready <N> --risk low|medium|high`).
- Keep one issue = one small, reviewable change (roughly one pull request).
-->

## Goal
What should be true when this is done, in one or two sentences.

## Context
Where the change goes and what exists today: relevant files, folders, functions,
screens or endpoints; current behavior vs. expected behavior; links to designs or docs.

## Scope
- ...

## Out of scope
- ...

## Acceptance criteria
- [ ] Concrete, checkable behavior (ideally something a test can prove).
- [ ] ...

## Dependencies
- #123
- #124 optional

## Verification
What should be checked, and which tests should cover it. The actual shell commands
live in the Autopilot config, not in this issue.
